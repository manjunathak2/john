import React, { useState } from "react";

const Header = () => {
  return (
    <div className='header'>
      <form>
        <input type='text' name='data' />
      </form>
    </div>
  );
};

export default Header;
